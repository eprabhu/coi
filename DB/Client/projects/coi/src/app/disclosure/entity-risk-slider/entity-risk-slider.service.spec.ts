/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { EntityRiskSliderService } from './entity-risk-slider.service';

describe('Service: EntityRiskSlider', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [EntityRiskSliderService]
    });
  });

  it('should ...', inject([EntityRiskSliderService], (service: EntityRiskSliderService) => {
    expect(service).toBeTruthy();
  }));
});
