/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { ActivateInactivateSfiModalService } from './activate-inactivate-sfi-modal.service';

describe('Service: ActivateInactivateSfiModal', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [ActivateInactivateSfiModalService]
    });
  });

  it('should ...', inject([ActivateInactivateSfiModalService], (service: ActivateInactivateSfiModalService) => {
    expect(service).toBeTruthy();
  }));
});
