/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { KeyPerformanceIndicatorProposalService } from './key-performance-indicator-proposal.service';

describe('Service: KeyPerformanceIndicatorProposal', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [KeyPerformanceIndicatorProposalService]
    });
  });

  it('should ...', inject([KeyPerformanceIndicatorProposalService], (service: KeyPerformanceIndicatorProposalService) => {
    expect(service).toBeTruthy();
  }));
});
