/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { AwardBasesalaryListResolverService } from './award-basesalary-list-resolver.service';

describe('Service: AwardBasesalaryListResolver', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [AwardBasesalaryListResolverService]
    });
  });

  it('should ...', inject([AwardBasesalaryListResolverService], (service: AwardBasesalaryListResolverService) => {
    expect(service).toBeTruthy();
  }));
});
