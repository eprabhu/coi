/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { AssignAdministratorModalService } from './assign-administrator-modal.service';

describe('Service: AssignAdministratorModal', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [AssignAdministratorModalService]
    });
  });

  it('should ...', inject([AssignAdministratorModalService], (service: AssignAdministratorModalService) => {
    expect(service).toBeTruthy();
  }));
});
